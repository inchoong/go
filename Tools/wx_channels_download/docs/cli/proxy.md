---
title: 启动代理服务命令
---

# 启动代理服务

核心功能，启动一个代理服务，拦截并修改视频号网络请求，达到插入下载按钮的目的

## 用法

```sh
wx_video_download --hostname 127.0.0.1 --port 2023 --debug
```

- `--hostname` 代理服务器主机名（默认 `127.0.0.1`）
- `--port` 代理服务器端口（默认 `2023`）
- `--debug` 是否开启调试输出

运行时会打印版本与问题反馈链接，并根据是否设置系统代理给出引导。
