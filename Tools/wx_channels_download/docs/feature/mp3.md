---
title: 下载mp3
---

# 下载 mp3

> 251226 版本后，必须安装 `ffmpeg` 才能下载 `mp3` 文件。或者在配置文件中指定 `download.frontend` 为 `true`，则会在前端转换 `mp3` 文件，无需本地安装 `ffmpeg`。

在「视频详情页」的「更多」菜单中，有「下载为 mp3」菜单，点击即可下载 `mp3` 文件

后台下载模式下，在下载面板中显示的文件大小，和实际的会不同，因为下载为 `mp3` 文件时，会先下载 `mp4` 文件，然后使用 `ffmpeg` 转换为 `mp3` 文件。
